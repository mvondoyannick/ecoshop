json.extract! supermarch, :id, :name, :logo, :email, :phone, :ville_id, :code, :quartier, :lieu_dit, :latitude, :longitude, :created_at, :updated_at
json.url supermarch_url(supermarch, format: :json)
